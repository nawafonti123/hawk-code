{
  "name": "hawk-code",
  "version": "0.1.0",
  "private": true,
  "description": "HAWK Code — AI Engineering Command Center",
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "turbo dev --filter=@hawk-code/desktop",
    "dev:desktop": "pnpm --filter @hawk-code/desktop tauri dev",
    "dev:web": "pnpm --filter @hawk-code/desktop dev",
    "dev:extension": "pnpm --filter @hawk-code/browser-extension build",
    "build": "turbo build",
    "build:desktop": "pnpm --filter @hawk-code/desktop tauri build",
    "build:extension": "pnpm --filter @hawk-code/browser-extension build",
    "modal:deploy": "pnpm modal:deploy:vision && pnpm modal:deploy:core",
    "modal:deploy:vision": "python -m modal deploy --env hawk-code infra/modal/vision.py",
    "modal:deploy:core": "python -m modal deploy --env hawk-code infra/modal/app_structured.py",
    "modal:prefetch:base": "python -m modal run --env hawk-code infra/modal/prefetch_base.py",
    "modal:serve": "python -m modal serve --env hawk-code infra/modal/app_structured.py",
    "modal:logs": "python -m modal app logs --env hawk-code hawk-code-ai",
    "modal:logs:vision": "python -m modal app logs --env hawk-code hawk-vision",
    "modal:stop": "python -m modal app stop --env hawk-code hawk-code-ai",
    "modal:test": "python -m modal run --env hawk-code infra/modal/app_structured.py",
    "modal:rollback:core": "python -m modal deploy --env hawk-code infra/modal/app_base.py",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "format:check": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "latest"
  }
}
